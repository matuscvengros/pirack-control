name: pirack-control
services:
  dashboard:
    image: ${PIRACK_IMAGE:-pirack-control}
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/home/pi/dashboard/data
    devices:
      - /dev/gpiomem:/dev/gpiomem
    restart: unless-stopped
